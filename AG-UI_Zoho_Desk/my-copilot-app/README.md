# Zoho Desk Copilot App

Frontend React (Next.js) + CopilotKit.

```bash
npm install
cp .env.example .env.local
npm run dev
```

Variables:
- `BACKEND_URL` — URL del backend ADK (default `http://localhost:8000`)
